var app;

function loadVue() {
	app = new Vue({
	    el: "#app",
	    data: {
			player,
			FORMS,
			UPGS,
			TABS,
			ACHS,
			AUTOS,
			CHALS,
			getDisplayShrink,
            format,
        }
	})
}